Connect the data files with the correct path in predict_match.

Put correct path in the input files.

Run it like a normal python code - python predict_match.py

Enter the match number you want, which is already in the 'Input' folder.
